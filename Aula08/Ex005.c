#include <stdio.h>
#include <stdlib.h>

int main()
{
    int valor, i;
    printf("Digite um valor para calcular seu fatorial: ");
    scanf("%d", &valor);
    i = 0;
    for (i = valor; i <= 0; i--)
    {
        printf(" %d", i);
    }
    
    return 0;
}